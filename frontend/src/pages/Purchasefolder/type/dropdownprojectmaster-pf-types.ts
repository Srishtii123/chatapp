export type TdropdownProjectmaster = {
  company_code?: string; // varchar(15): Unique identifier for the project (optional)
  project_code?: string; // varchar(200): Name of the project (optional)
  project_name?: string; // varchar(5): Company code (optional)
};
