export type TDivisionmaster = {
  company_code?: string; // varchar(15): Unique identifier for the project (optional)
  div_code?: string; // varchar(200): Name of the project (optional)
  div_name?: string; // varchar(5): Company code (optional)
};
