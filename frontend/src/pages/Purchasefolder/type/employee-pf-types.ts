export type TEmployeemaster = {
    company_code?: string; // varchar(15): Unique identifier for the project (optional)
    alternate_id?: string; // varchar(200): Name of the project (optional)
    rpt_name?: string; // varchar(5): Company code (optional)
  };