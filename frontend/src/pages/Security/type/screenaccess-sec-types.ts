export type TScreenaccess = {
  user_id: string;
  project_code: string;
  project_name: string;
};

export type TUserAssigned = {
  user_id: string;
  project_code: string;
};

export type TUser = {
  user_id: string;
  username: string;
};

export type TAssignedProject = {
  project_code: string;
  project_name: string;
};
