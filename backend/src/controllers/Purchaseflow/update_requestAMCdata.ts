import { Request, Response } from "express";

export const upsertAMCDetails = async (req: Request, res: Response) => {
  // Temporary stub to satisfy imports during build
  res.status(200).json({ success: true, message: "upsertAMCDetails stub" });
};
